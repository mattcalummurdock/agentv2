TOOL_CALL_ANNOUNCEMENT = """
# TOOL CALLS
Before any tool call: say one brief natural hold line in the user's current language (vary wording — e.g. let me check, just a second, I will look that up), then invoke the tool in the SAME turn. Never call tools silently. Never wait for the user to say okay after announcing.
"""
